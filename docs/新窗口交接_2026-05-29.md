# 新窗口交接提示词（2026-05-29）

把下面整段复制到新窗口，作为第一条消息发送。

```text
你现在接手我的 C:\Users\hp-pc\Desktop\deploy 项目。这是一个研电赛赛题五智能冰箱项目：RK3568 开发板 + 摄像头 + YOLOv8 RKNN 推理 + SQLite 库存 + Flask 后端 + Android App。请用中文和我沟通。

开始前必须先读：
1. AGENTS.md
2. PROJECT_CONTEXT.md
3. docs/国赛冲刺_端云协同与测试计划.md
4. docs/测试评估报告模板.md

但注意：PROJECT_CONTEXT.md 和部分 docs 里有过时信息，请以这条消息为准：

当前真实状态：
- 当前分支：feature/stability-optimization
- 板端部署目录：/root/2_deploy
- 开发板 IP：192.168.3.100
- 板子密码：topeet
- App 是另一个项目/窗口做的，这个窗口主要负责板端、后端、文档、测试方案、给 App 窗口写提示词
- 当前主模型应为：models/fridge_yolo_opset11_rknn16.rknn
- 板端必须使用：rknn-toolkit-lite2 1.6.0，/usr/lib/librknnrt.so 1.6.0
- 旧的 RKNN 1.4.0 会导致 YOLOv8 bbox 坐标分支异常，类别分数正常但前 4 个 bbox 坐标大量为 0。这个坑已经定位，不要再回退 1.4.0。
- 当前 CLASSES 已包含 egg，一共 20 类，不是旧文档里的 19 类。
- 当前后处理：输出 shape 可能是 (1,24,8400) 或 (1,24,8400,1)，解析为 (8400,24)，前 4 个是 xywh，后 20 个是类别分数，不要 sigmoid，不要乘 objectness。
- 当前预处理：RGB，uint8，NHWC 或 batch NHWC，例如 (1,640,640,3)，不要除以 255，不要传 data_format='nchw'。
- 当前 pytest 基线应是 37 passed。
- 已保存版本提交：b522e66 feat: stabilize rknn16 model and egg counting workflow

当前已实现能力：
1. 摄像头采集
2. 运动门控
3. 前后帧变化区域定位
4. YOLOv8 RKNN 本地识别
5. 库存增量更新
6. 包装食品 OCR 候选图保存
7. App 端 OCR 后写回包装库存
8. 包装物品取出：前后裁剪图交给 App OCR 判断
9. /api/cloud/confirm 云端兜底写回接口已经预留，但当前没有真正接云端大模型
10. --show 模式可以显示实时推理框、状态机状态、最近事件和变化区域框
11. 鸡蛋盒计数做过特殊增强：变化区域外扩 crop 后放大推理，置信度阈值降到 0.10，19 个鸡蛋目前大约能数到 18 个，可接受但要在报告中解释小目标误差和用户修正兜底

当前要继续做的任务：

任务 A：先帮我完成赛题答疑对齐补强
- 不要乱改核心链路，先整理报告和测试材料。
- 报告里要说明：RKNNLite 是 RK3568 平台上的轻量化端侧推理框架，类似 TFLite/NCNN/MNN 的端侧推理定位，但本项目实际使用 RKNNLite。
- 不要声称已经实测 TFLite、NCNN、MNN。
- 说明系统已实际运行轻量目标检测模型 YOLOv8n-RKNN。
- 分类由检测类别映射到粗分类完成。
- 分割目前没有真正 YOLO-seg 模型，不要虚假声称已经实现深度实例分割。
- 云端大模型不是必选。当前方案是端侧优先 + App OCR 协同增强 + 云端接口预留。
- 强调端侧负责事件触发和基础识别，关键是端侧不能漏事件。
- /api/cloud/confirm 可以作为低置信度或复杂包装识别的云端兜底接口说明。

任务 B：补低算力证据
- 整理 YOLOv8n 模型信息：参数量约 3,009,548，计算量约 8.1 GFLOPs，输入 640x640，RKNNLite 1.6.0。
- 需要指导我在板子上采集：单帧推理耗时、事件触发耗时、main.py --show 实时帧率、内存占用、CPU/NPU/GPU 使用情况。如果不好采集就至少采集推理耗时和帧率。
- 报告要说明系统不是每帧都跑完整模型，而是运动门控 + 稳定后事件触发推理，所以实际核心计算需求远低于 1 TOPS。

任务 C：辅助我做项目测试
你需要给我非常具体的测试步骤。比如我应该摆什么物体、怎么放入/取出、每个场景测几次、需要截图/录屏什么、哪些日志要复制给你、App 页面要截哪些图、失败要记录什么原因。

必须覆盖：
1. 生鲜单目标放入/取出：香蕉、苹果或番茄
2. 鸡蛋盒远距离放入/取出
3. 包装食品放入：牛奶、火锅底料等
4. 包装食品取出
5. 同时放入两个物体：香蕉 + 牛奶
6. 手伸进去但不取物
7. 整理位置但库存不变化
8. 低光照
9. 反光包装
10. 遮挡/堆叠场景，至少做到不漏事件
11. 用户手动修正名称/数量/保质期

任务 D：关于分割能力
我想在 YOLO 检测基础上增加分割能力，以便更好处理遮挡和紧贴场景。但我现在没有 mask 标注数据。
不要直接让我训练 YOLOv8-seg，风险太大。
请先评估并优先设计一个低风险方案：
- YOLO 检测框 + 前后帧变化区域 + OpenCV 传统分割
- 可考虑连通域、轮廓、GrabCut、Watershed
- 分割只作为二阶段辅助，不替代 YOLO 分类
- 用于判断一个大变化区域里可能有多个目标、目标紧贴、面积变化、遮挡
- 如果分割失败，必须回退现有 YOLO/OCR/用户修正逻辑
- 如果要写代码，先给方案和影响面，再动手
- 改核心模块后必须运行：python -m pytest tests/ -q

任务 E：文档清理
项目里很多文档还是旧状态，需要更新：
- fridge_yolo_v2.rknn 改成 fridge_yolo_opset11_rknn16.rknn
- RKNN 1.4.0 改成实际有效的 1.6.0
- 19 类改成 20 类，包含 egg
- pytest 基线从 25 passed 改成 37 passed
- 把 RKNN 1.4.0 bbox 坐标异常写成工程排障亮点
- 把单摄像头冰箱覆盖范围写成“监控取放通道/指定区域”，不要声称覆盖整个多层冰箱
- 多层冰箱作为扩展：多摄像头 + shelf_id

工作方式要求：
- 在修改代码前先读文件、查现状，不要猜。
- 不确定先问我。
- 不要大改已经能跑的主链路。
- 每次改完给我 scp 命令。
- 如果需要我在板子上操作，直接给我逐条命令和预期输出。
- 如果需要 App 窗口配合，给我一段可以直接复制过去的提示词。
- 你要主动辅助我做测试，不只是写代码。

请先不要写代码。请先：
1. 检查当前仓库状态和关键文件，确认你理解项目现状；
2. 给我一份“接下来两天测试与报告补强清单”；
3. 给我第一轮板端测试命令和记录模板。
```

## 当前 Git 保存状态

- 核心代码和 rknn16 模型已保存到提交：
  `b522e66 feat: stabilize rknn16 model and egg counting workflow`
- 不建议提交的中间产物：
  `models/fridge_yolo_v3.rknn`、`models/fridge_yolo_v4.rknn`、`models/fridge_yolo_v5.rknn`、`yolov8_model/weights/best2.pt`、`yolov8_model/weights/best2.onnx`
- 如果后续窗口需要清理工作区，先确认这些中间产物是否还要留作排障备份，不要直接删除。
